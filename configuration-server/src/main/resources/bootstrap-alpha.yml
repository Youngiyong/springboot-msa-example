# bootstrap-alpha.yml
spring:
  profile: alpha
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Youngiyong/Springboot-msa-project
          search-paths: server-configs