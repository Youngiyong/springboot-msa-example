# bootstrap-alpha.yml
spring:
  profile: alpha
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Youngiyong/springboot-msa-example
          search-paths: server-configs